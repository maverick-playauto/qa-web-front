import { ITextProp } from './Text.interface';
import * as S from './Text.style';

export const NormalText = ({
  id,
  children = 'empty',
  masking = '',
  inputType = 'primary',
  onChange,
  placeholder = '검색어 입력',
}: ITextProp) => {
  return <></>;
};
