import Container from 'features/workProgress/Container';

const WorkProgress = () => {
  return (
    <main>
      <h1>workProgress</h1>
      <Container />
    </main>
  );
};

export default WorkProgress;
